function SVG2Bitmap(b, d, f) {
    "use strict";
    if (f || (f = {}),
    (!f.scale || f.scale < 0) && (f.scale = 1),
    b && b.nodeName) {
        var m;
        if ("OBJECT" === b.nodeName || "IFRAME" === b.nodeName) {
            if (!b.contentDocument || "complete" === b.contentDocument.readyState && !b.contentDocument.documentElement)
                return void console.error("Unable to access the svg node : make sure it comes from the same domain or that the container has finished loading");
            if ("complete" !== b.contentDocument.readyState)
                return void b.addEventListener("load", r);
            b = (m = b).contentDocument.documentElement
        } else if ("EMBED" === b.nodeName && b.getSVGDocument && !(b = (m = b).getSVGDocument()))
            return m.addEventListener("load", r),
            m.onerror = function() {
                console.error("Unable to access the svg node : make sure it comes from the same domain or that the container has finished loading")
            }
            ,
            void (m.src = m.src);
        if ("svg" !== b.nodeName) {
            var e = b.querySelector("svg");
            if (!e) {
                var t = '[src*=".svg"]'
                  , n = b.querySelector("iframe" + t + ", embed" + t) || b.querySelector('object[data*=".svg"]');
                return n ? void SVG2Bitmap(n, d, f) : void console.error("unable to access the svg node, make sure it has been appended to the document")
            }
            b = e
        }
        var E, S = "http://www.w3.org/1999/xlink", x = "http://www.w3.org/2000/svg", C = b.cloneNode(!0), N = function() {
            var n = document.createElement("canvas")
              , r = n.getContext("2d");
            n.width = n.height = 1;
            var e = document.implementation.createHTMLDocument("test")
              , a = document.createElement("base");
            e.head.appendChild(a);
            var i = document.createElement("a");
            return e.body.appendChild(i),
            {
                isTainted: function(e) {
                    var t = !1;
                    r.drawImage(e, 0, 0);
                    try {
                        n.toDataURL()
                    } catch (e) {
                        t = !0,
                        n = n.cloneNode(!0),
                        r = n.getContext("2d")
                    }
                    return t
                },
                URL: function(e, t) {
                    return a.href = t,
                    i.href = e,
                    i
                }
            }
        }();
        !function() {
            var e, t = [], n = [], r = b.ownerDocument.styleSheets;
            for (e = 0; e < r.length; e++)
                n.push(r[e]);
            n.length && (A(),
            b.matches = b.matches || b.webkitMatchesSelector || b.mozMatchesSelector || b.msMatchesSelector || b.oMatchesSelector);
            for (e = 0; e < n.length; e++) {
                var a, i = n[e];
                try {
                    a = i.cssRules
                } catch (e) {
                    continue
                }
                for (var o = document.createElement("style"), l = a && a.length, s = 0; s < l; s++) {
                    var c = a[s].selectorText;
                    if (c && (b.matches && b.matches(c) || b.querySelector(c))) {
                        for (var h = a[s].cssText, u = new RegExp(/url\((.*?)\)/g), d = []; null !== (d = u.exec(h)); ) {
                            var m = d[1].replace(/\"/g, "")
                              , f = i.href || location.href;
                            t.push([m, f]);
                            var g = N.URL(m, f)
                              , v = f === location.href && m.indexOf(".svg") < 0 ? g.hash : g.href.substring(g.href.lastIndexOf("/") + 1)
                              , p = "#" + v.replace(/\//g, "_").replace(/\./g, "_").replace("#", "_");
                            h = h.replace(m, p)
                        }
                        o.innerHTML += h + "\n"
                    }
                }
                o.innerHTML && E.appendChild(o)
            }
            var w = C.style;
            w.border = w.padding = w.margin = 0,
            w.transform = "initial",
            function(e) {
                var f, g = 0, v = 0, p = [{
                    href: location.href.replace(location.hash, "").replace(/#/g, ""),
                    pathname: location.pathname,
                    filename: "",
                    innerElements: [],
                    parsedElements: [],
                    doc: b.ownerDocument,
                    base: location.href.replace(location.hash, "").replace(/#/g, "")
                }], w = function() {
                    var e = document.createElementNS(x, "use");
                    e.setAttributeNS(S, "href", "#__#"),
                    C.appendChild(e);
                    var t = !!C.querySelector('[*|href*="#"]');
                    return C.removeChild(e),
                    t
                }(), o = function(e, s) {
                    var r, t, n, a, i, o = [], l = function(e, t) {
                        var n, r = {};
                        if (r.element = e,
                        r.type = t,
                        r.attributes = [],
                        r.requestedElements = [],
                        r.parentDoc = s,
                        "xl" === t) {
                            if (!(n = e.attributes["xlink:href"])) {
                                var a = e.attributes.href;
                                if (!(a && a.namespaceURI && -1 < a.namespaceURI.indexOf("xlink")))
                                    return !1;
                                n = a
                            }
                            r.attributes.push(n),
                            r.requestedElements.push(n.value)
                        } else {
                            n = e.attributes;
                            for (var i = 0; i < n.length; i++)
                                for (var o = new RegExp(/url\((.*?)\)/g), l = []; null !== (l = o.exec(n[i].value)); )
                                    r.attributes.push(n[i]),
                                    r.requestedElements.push(l[1].replace(/"/g, ""))
                        }
                        return r
                    }, c = (r = e,
                    w ? r.querySelectorAll('[*|href*="#"]') : function() {
                        var e = []
                          , t = r.querySelectorAll("*");
                        for (f = 0; f < t.length; f++) {
                            var n = t[f].getAttributeNS(S, "href");
                            n && -1 < n.indexOf("#") && e.push(t[f])
                        }
                        return e
                    }()), h = (t = e,
                    n = "[*|" + ["style", "clip-path", "src", "cursor", "fill", "filter", "marker", "marker-start", "marker-mid", "marker-end", "mask", "stroke"].join('*="url"], *[*|') + '*="url"]',
                    t.querySelectorAll(n));
                    for (a = 0; a < c.length; a++)
                        (i = l(c[a], "xl")) && (o.push(i),
                        i = null);
                    for (a = 0; a < h.length; a++)
                        (i = l(h[a], "url")) && (o.push(i),
                        i = null);
                    var u = e.attributes;
                    for (a = 0; a < u.length; a++) {
                        var d = u[a];
                        if ("xlink:href" === d.name)
                            o.push(new l(e,"xl"));
                        else {
                            var m = d.value.match(/url\((.*)\)/);
                            m && 1 < m.length && o.push(new l(e,"url"))
                        }
                    }
                    return o
                }, l = function(e, t) {
                    for (var n = e.querySelectorAll("image"), r = 0; r < n.length; r++) {
                        var a = n[r].getAttributeNS(S, "href")
                          , i = N.URL(a, t).href;
                        a !== i && n[r].setAttributeNS(S, "href", i)
                    }
                }, y = function() {
                    var e;
                    for (e = 0; e < p.length; e++) {
                        var t = p[e];
                        if (t.doc) {
                            var n, r = t.innerElements;
                            if (r.length !== t.parsedElements.length)
                                for (n = 0; n < r.length; n++) {
                                    var a = t.doc.getElementById(r[n]);
                                    if (a) {
                                        var i = a.cloneNode(!0);
                                        i.id = t.filename + "_" + r[n],
                                        l(i, t.base),
                                        E.appendChild(i),
                                        s(o(i, t)),
                                        t.parsedElements.push(r[n]),
                                        g--
                                    } else
                                        console.warn("Couldn't find this element", r[n]),
                                        g--
                                }
                        }
                    }
                    v || g || M()
                }, c = function(e, t) {
                    for (var n = N.URL(e, t.base), r = n.href.substring(n.href.lastIndexOf("/") + 1).replace(n.hash, ""), a = r.replace(/\./g, "_"), i = n.hash.replace("#", ""), o = n.href.replace(n.hash, ""), l = a + "_" + i, s = 0; s < p.length; s++) {
                        var c = p[s];
                        if (c.href === o) {
                            if (0 === s) {
                                if (C.getElementById(i))
                                    return i;
                                l = "_" + i
                            }
                            return c.innerElements.indexOf(i) < 0 && (null !== c.doc ? g++ : console.warn("this element is also lost ", i),
                            c.innerElements.push(i)),
                            l
                        }
                    }
                    g++,
                    v++;
                    var h, u, d, m = {
                        href: o,
                        filename: a,
                        innerElements: [i],
                        parsedElements: []
                    };
                    return p.push(m),
                    u = (h = m).href,
                    (d = new XMLHttpRequest).onload = function() {
                        if (200 === this.status) {
                            var t = this.responseText || this.response;
                            if (!t)
                                return;
                            try {
                                h.doc = (new DOMParser).parseFromString(t, "text/html")
                            } catch (e) {
                                h.doc = document.implementation.createHTMLDocument(h.filename),
                                h.doc.body.innerHTML = t
                            }
                            h.base = u
                        } else
                            h.doc = null,
                            g -= h.innerElements.length,
                            console.warn("could not load this external document :", u, "\nThose elements are lost : ", h.innerElements.join(" , "));
                        --v || y()
                    }
                    ,
                    d.onerror = function(e) {
                        h.doc = null,
                        g -= h.innerElements.length,
                        console.warn("could not load this external document", u),
                        console.warn("Those elements are lost : ", h.innerElements.join(" , ")),
                        --v || y()
                    }
                    ,
                    d.open("GET", u),
                    d.send(),
                    l
                }, s = function(e) {
                    var t, n;
                    for (e.length && !E && A(),
                    t = 0; t < e.length; t++) {
                        var r = e[t];
                        for (n = 0; n < r.requestedElements.length; n++) {
                            var a = r.requestedElements[n]
                              , i = "#" + c(a, r.parentDoc)
                              , o = r.attributes[n]
                              , l = o.value.replace(a, i)
                              , s = o.name.toUpperCase() === o.name ? o.name.toLowerCase() : o.name;
                            r.element.setAttribute(s, l)
                        }
                    }
                };
                for (f = 0; f < e.length; f++)
                    c(e[f][0], {
                        base: e[f][1]
                    });
                s(o(C, p[0])),
                v || (g ? y() : M())
            }(t)
        }()
    } else
        console.error("Wrong arguments : should be \n SVG2Bitmap(SVGElement, function([canvasElement],[dataURL]) || IMGElement || CanvasElement [, {parameters})");
    function r() {
        this.removeEventListener("load", r),
        SVG2Bitmap(this, d, f)
    }
    function A() {
        (E = C.querySelector("defs") || document.createElementNS(x, "defs")).parentNode || C.insertBefore(E, C.firstElementChild)
    }
    function g() {
        var e, h = m ? m.getBoundingClientRect() : b.getBoundingClientRect();
        function t(e) {
            for (var t = Array.prototype.slice.call(e.attributes), n = 0; n < t.length; n++) {
                var r = t[n].name;
                -1 < r.indexOf(":") && r.indexOf("xlink") < 0 && e.removeAttribute(r)
            }
        }
        if (1 !== b.width.baseVal.unitType && C.setAttribute("width", h.width),
        1 !== b.height.baseVal.unitType && C.setAttribute("height", h.height),
        "undefined" != typeof ActiveXObject) {
            t(C);
            for (var n = C.querySelectorAll("*"), r = 0; r < n.length; r++)
                t(n[r])
        }
        C.removeAttribute("style"),
        e = (new XMLSerializer).serializeToString(C),
        f.svgModify && "function" == typeof f.svgModify && (e = f.svgModify(e));
        var a = "data:image/svg+xml; charset=utf8, " + encodeURIComponent(e)
          , u = new Image;
        u.onload = function t() {
            var e = d && "CANVAS" === d.nodeName ? d : document.createElement("canvas");
            if (e.originalSVG = m || b,
            e.width = h.width * f.scale,
            e.height = h.height * f.scale,
            e.width && e.height) {
                var n = f.scale
                  , r = this.width * n || e.width
                  , a = this.height * n || e.height
                  , i = e.getContext("2d");
                if (f.backgroundColor && (i.fillStyle = f.backgroundColor,
                i.fillRect(0, 0, r, a)),
                f.pattern) {
                    var o = i.createPattern(f.pattern, "repeat");
                    i.rect(0, 0, r, a),
                    i.fillStyle = o,
                    i.fill()
                }
                var l = m ? b.getBoundingClientRect() : {
                    top: 0,
                    left: 0
                };
                try {
                    if (i.drawImage(this, Math.round(f.offsetX || 0), Math.round(f.offsetY || 0), r, a, l.left, l.top, r, a),
                    f.watermark && f.watermark.img) {
                        var s = f.watermark.x || 0
                          , c = f.watermark.y || 0;
                        i.drawImage(f.watermark.img, 0, 0, f.watermark.img.width, f.watermark.img.height, s < 0 ? r + s : s, c < 0 ? a + c : c, f.watermark.img.width, f.watermark.img.height)
                    }
                } catch (e) {
                    setTimeout(t.bind(this), 200)
                }
                if (d || (d = function(e) {
                    var t = m || b;
                    t.parentNode.replaceChild(e, t)
                }
                ),
                N.isTainted(e))
                    return console.warn("Your browser has tainted the canvas."),
                    void ("IMG" === d.nodeName ? d.parentNode.replaceChild(e, d) : (e.setAttribute("style", v(e)),
                    d !== e && d.appendChild ? d.appendChild(e) : "function" == typeof d && d(e, null)));
                "IMG" === d.nodeName ? f.width && f.height ? (u.onload = function() {
                    try {
                        var e = document.createElement("canvas");
                        e.width = f.width,
                        e.height = f.height,
                        (i = e.getContext("2d")).drawImage(this, 0, 0, e.width, e.height, 0, 0, e.width, e.height)
                    } catch (e) {}
                    d.src = e.toDataURL(f.type, f.quality)
                }
                ,
                u.src = e.toDataURL(f.type, f.quality)) : (d.setAttribute("style", v(d)),
                d.src = e.toDataURL(f.type, f.quality)) : (e.setAttribute("style", v(e)),
                d !== e && d.appendChild ? d.appendChild(e) : "function" == typeof d && d(e, e.toDataURL(f.type, f.quality)))
            } else
                console.error("The document is not visible and can not be rendered")
        }
        ,
        u.onerror = function(e) {
            console.error("Couldn't export svg, please check that the svgElement passed is a valid svg document.")
        }
        ,
        u.src = a
    }
    function v(e) {
        var t = e.cloneNode(!0);
        b.parentNode.documentElement ? b.parentNode.documentElement.appendChild(t) : b.parentNode.insertBefore(t, b);
        for (var n = getComputedStyle(t), r = getComputedStyle(m || b), a = "", i = 0; i < r.length; i++)
            "width" !== r[i] && "height" !== r[i] && r[r[i]] !== n[r[i]] && (a += r[i] + ":" + r[r[i]] + ";");
        return t.parentNode.removeChild(t),
        a
    }
    function M() {
        var e, t = C.querySelectorAll("image"), n = t.length, r = 0;
        if (0 !== n) {
            var a = []
              , i = b.querySelectorAll("image");
            for (e = 0; e < t.length; e++)
                if (i[e] && i[e].isEqualNode(t[e]))
                    a.push(i[e]);
                else {
                    for (var o = null, l = 0; l < i.length; l++)
                        if (i[l].isEqualNode(t[e])) {
                            o = i[l];
                            break
                        }
                    a.push(o)
                }
            var s = document.createElement("canvas")
              , c = s.getContext("2d");
            for (e = 0; e < t.length; e++) {
                var h = t[e].getAttributeNS(S, "href");
                if (h && h.indexOf("data:image") < 0)
                    0 < h.indexOf(".svg") ? m(h, t[e]) : d(t[e], a[e]);
                else if (++r === n)
                    return void g()
            }
        } else
            g();
        function u(e) {
            var t = new Image;
            t.onload = function() {
                ++r === n && g()
            }
            ,
            t.src = e
        }
        function d(a, i) {
            var e = new Image
              , o = function() {
                console.warn("failed to load an image at : ", e.src),
                f.keepImageHolder || a.parentNode.removeChild(a),
                --n === r && g()
            };
            f.noCORS || (e.crossOrigin = "Anonymous"),
            e.onload = function() {
                var e, t;
                if (i && (e = a.getAttribute("preserveAspectRatio"),
                t = i.getBoundingClientRect()),
                i && t && (t.width * f.scale < this.width || t.height * f.scale < this.height)) {
                    s.width = t.width * f.scale,
                    s.height = t.height * f.scale;
                    var n = function(e, t, n) {
                        var c = e.width
                          , h = e.height
                          , u = t.width
                          , d = t.height;
                        function r(e) {
                            var t = h / c
                              , n = d / u
                              , r = t < n ? u : d / t
                              , a = t < n ? u * t : d;
                            function i(e, t, n) {
                                var r = Math.max(t, n)
                                  , a = Math.min(t, n);
                                switch (e) {
                                case "Min":
                                    return 0;
                                case "Mid":
                                    return (r - a) / 2;
                                case "Max":
                                    return r - a;
                                default:
                                    return "invalid"
                                }
                            }
                            var o = [m, 0, 0, c, h, i(e[0], r, u), i(e[1], a, d), r, a];
                            return "invalid" === o[5] || "invalid" === o[6] ? f : o
                        }
                        var m = "IMG" === e.nodeName || "VIDEO" === e.nodeName || "CANVAS" === e.nodeName ? e : null
                          , f = r(["Mid", "Mid"]);
                        {
                            if (!n)
                                return f;
                            var a = n.trim().split(" ")
                              , i = a[0].replace("x", "").split("Y");
                            switch (a[a.length - 1]) {
                            case "meet":
                                return r(i);
                            case "slice":
                                return function(e) {
                                    var t, n;
                                    function r() {
                                        n = h * (t = u) / c
                                    }
                                    function a() {
                                        t = c * d / h,
                                        n = d
                                    }
                                    d < u ? (r(),
                                    n < d && a()) : u === d ? h < c ? a() : r() : (a(),
                                    t < u && r());
                                    function i(e, t, n, r) {
                                        switch (e) {
                                        case "Min":
                                            return 0;
                                        case "Mid":
                                            return (t - n) / 2 * r / t;
                                        case "Max":
                                            return (t - n) * r / t;
                                        default:
                                            return "invalid"
                                        }
                                    }
                                    var o = i(e[0], t, u, c)
                                      , l = i(e[1], n, d, h)
                                      , s = [m, o, l, c - o, h - l, 0, 0, t - o * (t / c), n - l * (n / h)];
                                    return "invalid" !== s[1] && "invalid" !== s[2] ? s : f
                                }(i);
                            default:
                                return f
                            }
                        }
                    }(this, s, e);
                    c.drawImage.apply(c, n)
                } else
                    s.width = this.width,
                    s.height = this.height,
                    c.drawImage(this, 0, 0);
                if (N.isTainted(s))
                    o();
                else {
                    var r = s.toDataURL();
                    a.setAttributeNS(S, "href", r),
                    u(r)
                }
            }
            ,
            e.onerror = function() {
                var e = this.src;
                this.onerror = o,
                this.removeAttribute("crossorigin"),
                this.src = "",
                this.src = e
            }
            ,
            e.src = a.getAttributeNS(S, "href")
        }
        function m(e, n) {
            var t = new XMLHttpRequest;
            t.onload = function() {
                if (200 === this.status) {
                    var e = this.responseText || this.response
                      , t = "data:image/svg+xml; charset=utf8, " + encodeURIComponent("<svg" + e.split("<svg")[1]);
                    n.setAttributeNS(S, "href", t),
                    u(t)
                } else
                    d(n)
            }
            ,
            t.onerror = function() {
                d(n)
            }
            ;
            try {
                t.open("GET", e)
            } catch (e) {
                return void d(n)
            }
            t.send()
        }
    }
}
