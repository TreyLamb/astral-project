We are creating a League of Legends item building tool similar to the ones that are on the league of legends client and other websites do you need to do research to see what I'm talking about so that you understand clearly what we're building. I want to be able to easily choose my items for my build and as I choose the items they go into a secondary window like apip window that is borderless so it looks like it's a floating app window. 

The build should look very similar to League of Legends current client where there's new blocks you can choose items with a filter I want all of the filters that League of Legends currently has for items and then we will add our own I want a way to be able to save builds based on character. The filters that we add will be done by using unique labels which we can attach a label to an item we can attach multiple items label to an item then if we use the filter and we choose the specific label within the filter all of those items with that label will be filtered in

Do not worry about prunes or spells we are focusing only on items. The app pop out the PIP window should be able to hold as many new blocks as I want and each block should show in column view in the website the normal page should show things in rows so if I want to go into the the picture in picture view and have two blocks and add another I can add up to any as many as I want the window size should be resizable but it should always resize itself to fit as many blocks as in the window at least . If I size it larger that doesn't matter but if I size it smaller the blocks should resize themselves to fit the window for how small it is in fact the blocks should just always resize to the size of the window so if I have a big window they're large if I have a small window they're small above the blocks in the pop out window I want a little comments column or square above each block and the input for these notes are in the main page next to the respective block in the pop out window it is uneditable.The whole window is view-only. It should be sleeck and fairly basic and smooth.

There will be 123 example images in the league build repo so you have a visual of what a basic item build looks like put these into their own folder after viewing them
